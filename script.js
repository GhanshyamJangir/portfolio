const menuBtn=document.getElementById("menuBtn"),navLinks=document.getElementById("navLinks"),topBtn=document.getElementById("topBtn");menuBtn.addEventListener("click",()=>{navLinks.classList.toggle("active");menuBtn.textContent=navLinks.classList.contains("active")?"✕":"☰"});document.querySelectorAll(".links a").forEach(a=>a.addEventListener("click",()=>{navLinks.classList.remove("active");menuBtn.textContent="☰"}));window.addEventListener("scroll",()=>{window.scrollY>350?topBtn.classList.add("show"):topBtn.classList.remove("show")});topBtn.addEventListener("click",()=>window.scrollTo({top:0,behavior:"smooth"}));